You are checking whether resume section headers are ATS-friendly.

Review only the extracted resume text below. Determine whether standard section headers are recognizable as plain text, such as "Experience", "Projects", "Education", "Skills", "Summary", or similar.

Flag likely problems such as headers represented only by icons, decorative symbols, unusual punctuation, or vague/stylized labels that an ATS may not recognize.

Return JSON only:
{
  "passed": true,
  "detail": "string",
  "suggested_fix": "string"
}

If the check passes, use an empty string for suggested_fix.

Extracted resume text:
{{resume_text}}
